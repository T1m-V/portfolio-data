[project]
name = "portfolio-data-workspace"
version = "0.3.0"
description = "Versioned market data and private local state for the portfolio tools"
requires-python = ">=3.14,<3.15"
dependencies = [
    "portfolio-crypto-data @ git+https://github.com/T1m-V/portfolio-crypto-data.git@v0.3.0",
    "portfolio-market-data @ git+https://github.com/T1m-V/portfolio-market-data.git@v0.3.0",
]

[tool.uv]
package = false
required-version = ">=0.12.3,<0.13"
preview-features = ["centralized-project-envs"]
